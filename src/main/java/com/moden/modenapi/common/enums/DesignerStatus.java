package com.moden.modenapi.common.enums;

public enum DesignerStatus {
    WORKING,        // 근무중
    VACATION,       // 휴가(ta'tilda)
    TRAINING,       // 교육/연수(malaka oshirish)
    LEAVE           // 휴직(uzoq muddatli)
}
